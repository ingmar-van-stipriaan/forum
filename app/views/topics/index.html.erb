<div class="row">
  <div class="col-xs-12">
    <h1 class="page-header">Topics</h1>
  </div>
</div>
<div class="row">
  <div class="col-xs-12">
    <%= link_to "Nieuwe Topic", new_topic_path, :user_id => current_user.id, :class => "btn btn-success" %>
  </div>
</div>
<div class="row">
  <div class="col-xs-12">
    <table class="table table-striped table-border">
      <thead>
        <tr>
          <th>Titel</th>
          <th>Aantal berichten</th>
          <th>Aangemaakt op</th>
          <th>Laaste bericht</th>
          <th>Aangemaakt door</th>
        </tr>
      </thead>
      <tbody>
        <% @topics.each do |topic| %>
          <tr>
            <td><%= link_to topic.title, topic_path(topic.id) %></td>
            <td><!-- show messages count --></td>
            <td><%= format_date(topic.created_at) %></td>
            <td><%= format_date(topic.updated_at) %></td>
            <td><%= topic.user.email if topic.user %></td>
            <td>
              <% if topic.user.id == current_user.id %>
                <%= link_to "Bewerk", edit_topic_path(topic.id), :class => "btn btn-default" %>
                <%= link_to "Verwijder", topic_path(topic.id), :method => :delete, :confirm => "Weet u dit zeker?", :class => "btn btn-danger" %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
